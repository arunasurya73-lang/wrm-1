services:
  - type: web
    name: airsense-delhi
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node backend.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_VERSION
        value: 20
